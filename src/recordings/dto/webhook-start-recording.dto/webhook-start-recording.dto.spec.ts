import { WebhookStartRecordingDto } from './webhook-start-recording.dto';

describe('WebhookStartRecordingDto', () => {
  it('should be defined', () => {
    expect(new WebhookStartRecordingDto()).toBeDefined();
  });
});
