import { UpdateConferenceDto } from './update-conference.dto';

describe('UpdateConferenceDto', () => {
  it('should be defined', () => {
    expect(new UpdateConferenceDto()).toBeDefined();
  });
});
