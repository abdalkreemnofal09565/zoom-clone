import { CreateConferenceDto } from './create-conference.dto';

describe('CreateConferenceDto', () => {
  it('should be defined', () => {
    expect(new CreateConferenceDto()).toBeDefined();
  });
});
