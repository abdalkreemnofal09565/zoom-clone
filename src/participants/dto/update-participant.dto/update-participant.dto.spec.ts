import { UpdateParticipantDto } from './update-participant.dto';

describe('UpdateParticipantDto', () => {
  it('should be defined', () => {
    expect(new UpdateParticipantDto()).toBeDefined();
  });
});
